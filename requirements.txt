# Runtime dependencies (mirrors setup.py install_requires)
paramiko>=3.4,<4
paramiko-expect>=0.3.5
colorama>=0.4.6
cryptography>=42.0
PyYAML>=6.0
pyserial>=3.5
# mergedb: resolved from the 2019 editable VCS pin to the maintained PyPI release.
mergedb>=0.1.1
# EdgeOS/Vyatta config parsing used by the EdgeOS driver.
VyattaConfParser>=0.5.1
# HTTP client for the Omada controller Northbound REST protocol.
requests>=2.31

# Test dependencies
coverage>=7.4
mock>=5.1
pytest>=8.0

# Doc dependencies
Sphinx>=7.2
sphinx-rtd-theme>=2.0
sphinxcontrib-programoutput>=0.17

# Packaging/release dependencies
build>=1.2
twine>=5.0
setuptools-scm>=8.0
